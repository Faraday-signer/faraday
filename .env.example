# Copy to .env (gitignored — NEVER commit the real values).
# Telegram board bot (@faraday_board_bot) — see docs/telegram-board.md.
# Ask cxalem for the token, chat id, and pin message id over a private DM.
TG_BOT_TOKEN=
TG_CHAT_ID=
TG_PIN_MSG_ID=
# Optional: name stamped on your posts (defaults to `git config user.name`).
# TG_AUTHOR=
