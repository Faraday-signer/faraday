# 2026-07-20 — Five team decisions applied: FA-02 dual BOM, FA-16 approver, new FA-17, FA-08 owner, PR #41 closed

Decisions from the team (cxalem), applied to `docs/backlog.md`:

1. **FA-02 re-scoped to a dual BOM** — the per-device cost estimate now covers both the Raspberry Pi Zero build and the ESP32-S3 build (the migration made a Pi-only estimate stale). Acceptance criteria require per-unit costs at 1/10/50 for both variants.
2. **FA-16 design approver named** — cxalem (Alejandro) approves the design direction on a preview before the full build/merge. Written into the card's plan and acceptance criteria.
3. **New card FA-17 `P1` — ESP32-S3 release artifact: versioned firmware image + ESP Web Tools manifest.** Release/CI plumbing (firmware already runs on ESP32-S3 per Nahem and Javi): release workflow builds a merged flashable image from the xtensa build (needs PRs #73/#93 merged), versions it, emits the ESP Web Tools manifest, publishes both as release assets. Starts with a timeboxed spike (espflash save-image / merged-bin format, manifest schema) since this is new ground for the team. Product/eng, Backlog. **FA-13 now depends on FA-17** (plus FA-16 and PRs #73/#93). Acceptance includes that the released image is the same firmware the CI nm radio audit gates — the no-radio-symbols property holds for what users flash.
4. **FA-08 owner assigned: Trskel (Javi Lois** — handle resolved from git authors, `javilois@gmail.com`). Board notes that per the claiming protocol the claim becomes real when his draft PR titled `FA-08` opens.
5. **PR #41 (payroll dashboard) — decided: close.** cxalem is closing it directly on GitHub. No card needed; recorded here so the decision isn't tribal. The stale open PR list is now: #26 (folded into FA-10's triage) and the ESP32 pair #73/#93 (tracked via FA-17/FA-13 dependencies).

**Verification:** board index and card bodies updated consistently (FA-17 inserted before FA-13 in both); numbering continues the shared sequence (FA-17 is the new high-water mark); no ID collides with an open branch/PR title.

**Not committed** — working-tree changes only, per the requester's instruction.
